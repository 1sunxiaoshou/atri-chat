#项目优化

- 供应商前端展示与后端存储不一致。
优化思路：
统一字段
供应商名称（自定义）、供应商类别（受支持的供应商）、供应商描述（受支持的供应商类内置）、供应商可配置项（api_key、base_url）
供应商名称和可配置项存储在数据库中，其他的直接在代码中。允许用户创建多个名称不同，但供应商类别相同的。

- 模型管理页面优化。
优化思路：
支持筛选、按照供应商、模型类别进行单一或组合筛选。

- 角色管理优化。
角色头像允许通过上传图像或者url来配置，将这个信息保存到数据库中