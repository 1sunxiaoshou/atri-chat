# TTS (文本转语音) 配置文件

# 默认使用的提供商
default_provider: gpt_sovits

# 提供商配置
providers:
  # GPT-SoVITS
  gpt_sovits:
    enabled: true
    api_url: http://localhost:9880
    refer_wav_path: "E:\\AI_Music\\GPT-SoVITS-1007-cu124\\models\\v4\\原神-中文-早柚_ZH\\reference_audios\\中文\\emotions\\【默认】我待在这里，会很不自在。.wav"
    prompt_text: "我待在这里，会很不自在。"
    prompt_language: zh
    text_language: zh

    
  # OpenAI TTS (预留)
  openai:
    enabled: false
    api_key: ""
    base_url: ""
    model: tts-1  # tts-1 或 tts-1-hd
    voice: alloy  # alloy, echo, fable, onyx, nova, shimmer
    speed: 1.0
